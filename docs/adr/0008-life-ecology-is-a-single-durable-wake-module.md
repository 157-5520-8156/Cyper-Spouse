# Life Ecology 以单一 durable wake Module 推进

状态：已接受。World v2 选择以 `LifeEcologyRuntime` 将一次已提交的 clock/life wake 转换为受模型审议、Proposal/Acceptance 验证和可回放的生活变化，并在事实提交后调用 source-bound 媒体候选生态；不采用每类图片独立 scheduler，也不将用户 Fact 扩展为角色环境事实。这个决定集中事实、隐私、随机、恢复和媒体隔离的复杂性，同时保留模型在分类矩阵内选择、延后、改期或 no-op 的自由。
